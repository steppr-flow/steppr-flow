services:
  # Zookeeper (required for Confluent Kafka)
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: steppr-flow-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - steppr-flow-network

  # Apache Kafka (Confluent Platform)
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: steppr-flow-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
    volumes:
      - kafka-data:/var/lib/kafka/data
    depends_on:
      - zookeeper
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-dashboard localhost:9092 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - steppr-flow-network

  # MongoDB
  mongodb:
    image: mongo:7.0
    container_name: steppr-flow-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: stepprflow
    volumes:
      - mongodb-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
    networks:
      - steppr-flow-network

  # Mongo Express (MongoDB GUI)
  mongo-express:
    image: mongo-express:latest
    container_name: steppr-flow-mongo-express
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_SERVER: mongodb
      ME_CONFIG_MONGODB_PORT: 27017
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - steppr-flow-network

  # RabbitMQ
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: steppr-flow-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - steppr-flow-network

  # Steppr Flow Dashboard (Monitoring Server)
  steppr-flow-dashboard:
    build:
      context: .
      dockerfile: steppr-flow-dashboard/Dockerfile
    container_name: steppr-flow-dashboard
    ports:
      - "8090:8090"
    environment:
      SERVER_PORT: 8090
      MONGODB_URI: mongodb://mongodb:27017/stepprflow
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      KAFKA_CONSUMER_GROUP: steppr-flow-dashboard
      KAFKA_CONSUMER_CONCURRENCY: 3
      LOG_LEVEL: INFO
      JAVA_OPTS: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC"
    depends_on:
      mongodb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8090/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - steppr-flow-network

  # Steppr Flow UI (Vue.js frontend served by nginx)
  steppr-flow-ui:
    build:
      context: ./steppr-flow-ui
      dockerfile: Dockerfile
    container_name: steppr-flow-ui
    ports:
      - "3000:80"
    environment:
      API_URL: http://steppr-flow-dashboard:8090
    depends_on:
      steppr-flow-dashboard:
        condition: service_healthy
    networks:
      - steppr-flow-network

volumes:
  kafka-data:
  mongodb-data:
  rabbitmq-data:

networks:
  steppr-flow-network:
    driver: bridge
